import React from "react";

const Header = () => {
    return(
        <div style={{marginBottom: "1em"}}>
            <h1 className="ui center aligned block header">Contact Manager</h1>
        </div>
    );
};

export default Header;